# Formatter

Vue 3 + Vite + Tailwind app to minify / beautify JSON, XML, and JavaScript.

## Setup

```bash
npm install
npm run dev
```

Then open the URL Vite prints (usually http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```
